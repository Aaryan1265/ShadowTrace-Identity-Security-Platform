# Local development defaults to SQLite.
DATABASE_URL=sqlite:///./shadowtrace.db

# Set this in a real deployment to protect POST /api/events and /api/events/batch.
# SHADOWTRACE_API_KEY=replace-with-a-long-random-value

APP_ENV=development
